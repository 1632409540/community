<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xzy.community.mapper.QuestionExtMapper">
  <resultMap id="BaseResultMap" type="com.xzy.community.model.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sun May 24 20:08:21 CST 2020.
    -->
    <id column="ID" jdbcType="BIGINT" property="id" />
    <result column="TITLE" jdbcType="VARCHAR" property="title" />
    <result column="GMT_CREATE" jdbcType="BIGINT" property="gmtCreate" />
    <result column="GMT_MODIFIED" jdbcType="BIGINT" property="gmtModified" />
    <result column="CREATOR" jdbcType="BIGINT" property="creator" />
    <result column="COMMENT_COUNT" jdbcType="INTEGER" property="commentCount" />
    <result column="VIEW_COUNT" jdbcType="INTEGER" property="viewCount" />
    <result column="LIKE_COUNT" jdbcType="INTEGER" property="likeCount" />
    <result column="TAG" jdbcType="VARCHAR" property="tag" />
  </resultMap>

  <update id="addViewCount" parameterType="com.xzy.community.model.Question">
    update question
    set VIEW_COUNT = VIEW_COUNT + #{viewCount,jdbcType=INTEGER}
    where ID = #{id,jdbcType=INTEGER}
  </update>
  <update id="addCommentCount" parameterType="com.xzy.community.model.Question">
    update question
    set COMMENT_COUNT = COMMENT_COUNT + #{commentCount,jdbcType=INTEGER}
    where ID = #{id,jdbcType=INTEGER}
  </update>

  <select id="countBySearchAndTagExceptId" parameterType="com.xzy.community.dto.QuestionQueryDTO" resultType="java.lang.Integer">
    select count(*) from question
    <where>
      <if test="id !=null and id !=0">
        and id != #{id}
      </if>
      <if test="search != null and search.length > 0">
        and title regexp #{search}
      </if>
      <if test="tag !=null and tag.length > 0">
        and tag regexp #{tag}
      </if>
    </where>
  </select>

  <select id="selectBySearchAndTagExceptId" parameterType="com.xzy.community.dto.QuestionQueryDTO" resultMap="BaseResultMap">
    select * from question
    <where>
      <if test="id !=null and id !=0">
        and id != #{id}
      </if>
      <if test="search != null and search.length > 0">
        and title regexp #{search}
      </if>
      <if test="tag!=null and tag.length > 0">
        and tag regexp #{tag}
      </if>
    </where>
    order by gmt_create desc limit #{page},#{size}
  </select>

  <select id="getCommentCountByTag" parameterType="java.lang.String" resultType="java.lang.Integer">
    select sum(COMMENT_COUNT) from question where TAG like #{tag}
  </select>
</mapper>